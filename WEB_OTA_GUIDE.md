# 📤 Оновлення прошивки через веб-інтерфейс

## ✅ Швидка інструкція

### Крок 1: Зібрати прошивку
```bash
cd ~/Documents/esp32
pio run
```

### Крок 2: Знайти файл прошивки
Після збірки файл прошивки знаходиться тут:
```
.pio/build/lilygo-t-display/firmware.bin
```

### Крок 3: Відкрити веб-інтерфейс
1. Відкрийте браузер
2. Перейдіть на IP адресу ESP32 (наприклад: `http://11.18.10.55`)
3. Натисніть кнопку **"📤 Прошивка"**

### Крок 4: Завантажити прошивку
1. Натисніть **"Виберіть файл"**
2. Виберіть файл `.pio/build/lilygo-t-display/firmware.bin`
3. Натисніть **"📤 Завантажити та встановити"**
4. Дочекайтеся завершення (показується прогрес)
5. ESP32 автоматично перезапуститься після встановлення

---

## 📋 Детальна інструкція

### 1. Збірка прошивки

```bash
cd ~/Documents/esp32
pio run
```

**Очікуваний результат:**
```
Building .pio/build/lilygo-t-display/firmware.bin
```

### 2. Перевірка файлу прошивки

```bash
ls -lh .pio/build/lilygo-t-display/firmware.bin
```

**Очікуваний результат:**
```
-rw-r--r-- 1 user user 1.2M Dec  1 12:00 .pio/build/lilygo-t-display/firmware.bin
```

### 3. Відкриття веб-інтерфейсу

**Варіант A: Через IP адресу**
- Відкрийте браузер
- Введіть IP адресу ESP32 (наприклад: `http://11.18.10.55`)
- Або знайдіть IP в Serial Monitor або на дисплеї ESP32

**Варіант B: Через hostname (якщо налаштовано)**
- Відкрийте браузер
- Введіть: `http://ESP32-Bluetti-EB3A.local`

### 4. Завантаження прошивки

1. На головній сторінці натисніть кнопку **"📤 Прошивка"**
2. На сторінці оновлення:
   - Натисніть **"Виберіть файл"**
   - Знайдіть та виберіть файл `.pio/build/lilygo-t-display/firmware.bin`
   - Натисніть **"📤 Завантажити та встановити"**
3. Спостерігайте за прогресом:
   - Показується відсоток завантаження
   - Після завершення: "✅ Завантаження завершено! Встановлення..."
4. ESP32 автоматично перезапуститься через 5 секунд

---

## ⚠️ Важливо

### Обмеження розміру файлу
- Максимальний розмір: **1.3 MB** (1310720 байт)
- Якщо файл більший, завантаження не почнеться

### Перевірка підключення
- Переконайтеся, що ESP32 підключений до WiFi
- Переконайтеся, що веб-інтерфейс доступний
- Не закривайте браузер під час завантаження

### Після оновлення
- ESP32 автоматично перезапуститься
- Після перезапуску перевірте, чи працює нова прошивка
- Якщо щось пішло не так, можна завантажити стару прошивку через USB

---

## 🔧 Усунення проблем

### Помилка "File too large"
**Причина:** Файл прошивки більше 1.3 MB

**Рішення:**
- Перевірте розмір файлу: `ls -lh .pio/build/lilygo-t-display/firmware.bin`
- Спробуйте оптимізувати код або видалити непотрібні бібліотеки

### Помилка "Update failed"
**Причина:** Помилка під час запису прошивки

**Рішення:**
- Переконайтеся, що ESP32 має достатньо вільної пам'яті
- Перевірте підключення WiFi (стабільне)
- Спробуйте завантажити через USB

### Завантаження зависає
**Причина:** Нестабільне підключення WiFi

**Рішення:**
- Переконайтеся, що ESP32 близько до роутера
- Перевірте RSSI (має бути > -70 dBm)
- Спробуйте завантажити через USB

---

## 📊 Переваги веб-інтерфейсу

✅ **Не потрібен USB кабель**
✅ **Можна оновити віддалено**
✅ **Швидко та зручно**
✅ **Візуальний прогрес завантаження**
✅ **Автоматичний перезапуск**

---

## 🔄 Альтернативні методи

### Метод 1: PlatformIO OTA
```bash
pio run --target upload --upload-port 11.18.10.55
```

### Метод 2: USB (якщо веб не працює)
```bash
pio run --target upload
```

---

## 💡 Поради

1. **Завжди зберігайте робочу прошивку** - якщо щось піде не так, можна завантажити через USB
2. **Перевіряйте розмір файлу** перед завантаженням
3. **Не вимикайте ESP32** під час оновлення
4. **Дочекайтеся завершення** - не закривайте браузер до завершення




