# 🔌 Віддалене завантаження прошивки (ручний спосіб)

## Проблема з автоматичним скриптом

SSH потребує пароль, але автоматичне введення не працює. Використовуйте один з цих способів:

## Спосіб 1: Простий скрипт (рекомендовано)

```bash
cd /home/kekcick/Documents/esp32
./remote_upload_simple.sh
```

Скрипт запитає пароль, якщо потрібно.

## Спосіб 2: Вручну через SSH

### Крок 1: Синхронізуйте код

```bash
rsync -avz --exclude '.pio' --exclude '.git' --exclude '*.bin' \
  /home/kekcick/Documents/esp32/ \
  user@11.18.10.40:~/esp32/
```

### Крок 2: Підключіться до сервера

```bash
ssh user@11.18.10.40
```

### Крок 3: Виконайте завантаження на сервері

```bash
cd ~/esp32
pio run --target upload --upload-port /dev/ttyACM0
```

## Спосіб 3: Налаштуйте SSH ключі (для автоматизації)

### Крок 1: Генеруйте ключ

```bash
ssh-keygen -t rsa -b 4096
```

### Крок 2: Скопіюйте на сервер

```bash
ssh-copy-id user@11.18.10.40
# Введіть пароль один раз
```

### Крок 3: Перевірте

```bash
ssh user@11.18.10.40 "echo 'SSH ключ працює!'"
# Тепер не повинен запитувати пароль
```

Після цього `./remote_upload.sh` працюватиме автоматично!

## Спосіб 4: Використайте sshpass (небезпечно, не рекомендовано)

```bash
# Встановіть sshpass
sudo apt-get install sshpass

# Використання (НЕ БЕЗПЕЧНО - пароль в командному рядку!)
sshpass -p 'your_password' ssh user@11.18.10.40 "cd ~/esp32 && pio run --target upload --upload-port /dev/ttyACM0"
```

---

**Рекомендація:** Налаштуйте SSH ключі (Спосіб 3) - це найбезпечніше та найзручніше!

