# 🔐 Налаштування SSH для віддаленого завантаження

## Проблема

Скрипт показує помилку:
```
❌ Не вдалося підключитися до 11.18.10.40
Permission denied (publickey,password)
```

Це означає, що потрібно налаштувати SSH доступ.

## Рішення 1: Налаштувати SSH ключі (рекомендовано)

### Крок 1: Генеруйте SSH ключ (якщо немає)

```bash
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
# Натисніть Enter для використання стандартного розташування
# Введіть пароль для ключа (або залиште порожнім)
```

### Крок 2: Скопіюйте ключ на сервер

```bash
ssh-copy-id userr@11.18.10.40
# Введіть пароль один раз
```

### Крок 3: Перевірте підключення

```bash
ssh userr@11.18.10.40 "echo 'SSH ключ працює!'"
# Тепер не повинен запитувати пароль
```

## Рішення 2: Використовувати пароль (тимчасово)

Якщо не хочете налаштовувати ключі, можна використовувати пароль:

### Варіант A: Модифікувати скрипт для інтерактивного введення

Скрипт вже підтримує інтерактивне введення пароля, але SSH тунель потребує безпарольного доступу.

### Варіант B: Використати sshpass (небезпечно, не рекомендовано)

```bash
# Встановіть sshpass
sudo apt-get install sshpass

# Використання (НЕ БЕЗПЕЧНО - пароль в командному рядку!)
sshpass -p 'your_password' ssh userr@11.18.10.40
```

## Рішення 3: Виконати команди на сервері напряму

Якщо SSH ключі не налаштовані, можна виконати завантаження на сервері:

### Крок 1: Синхронізуйте код

```bash
# Через rsync
rsync -avz --exclude '.pio' --exclude '.git' \
  /home/kekcick/Documents/esp32/ \
  userr@11.18.10.40:~/esp32/

# Або через scp
scp -r /home/kekcick/Documents/esp32 userr@11.18.10.40:~/
```

### Крок 2: Виконайте на сервері

```bash
ssh userr@11.18.10.40
cd ~/esp32
pio run --target upload
```

## Перевірка поточного стану

### Перевірте SSH доступ:

```bash
ssh userr@11.18.10.40 "echo 'Connected!'"
```

### Перевірте чи є SSH ключ:

```bash
ls -la ~/.ssh/id_rsa.pub
```

### Перевірте чи ключ скопійовано:

```bash
ssh userr@11.18.10.40 "cat ~/.ssh/authorized_keys | grep $(cat ~/.ssh/id_rsa.pub | cut -d' ' -f2)"
```

## Рекомендації

1. **Використовуйте SSH ключі** - безпечніше та зручніше
2. **Не зберігайте паролі в скриптах** - це небезпечно
3. **Обмежте доступ до ключів** - `chmod 600 ~/.ssh/id_rsa`

---

**Після налаштування SSH ключів, скрипт `./remote_upload.sh` працюватиме автоматично!** ✅

