# 🔴 ПРОБЛЕМА: Bootloop

ESP32 постійно перезавантажується - виявлена критична помилка.

## Причина
Crash через `rst:0x3 (SW_RESET)` - ймовірно:
1. Помилка ініціалізації NimBLE
2. Проблема з дисплеєм TFT_eSPI
3. Недостатньо пам'яті

## Рішення
Потрібно спростити код або подивитись повний лог помилки.

Натисніть Ctrl+C в терміналі монітора, потім запустіть:
```bash
pio device monitor --baud 115200 --filter direct
```

Це покаже повний стек trace помилки.
