# 📡 OTA (Over-The-Air) Оновлення

## 🎯 Що таке OTA?

**OTA (Over-The-Air)** - це можливість оновлювати прошивку ESP32 **через WiFi** без USB кабелю.

### Переваги:
- ✅ Не потрібен USB кабель
- ✅ Можна оновити віддалено
- ✅ Швидше та зручніше
- ✅ ESP32 може бути в будь-якому місці (в межах WiFi)

---

## 🚀 Перше завантаження (через USB)

**Перший раз** потрібно завантажити прошивку через USB:

```bash
cd ~/Documents/esp32
make upload
# або
pio run --target upload
```

Після цього OTA буде доступний автоматично!

---

## 📡 Оновлення через WiFi

### Метод 1: PlatformIO (рекомендовано)

```bash
# Показати доступні пристрої
pio device list

# Ви побачите щось на зразок:
# ESP32-Bluetti-EB3A.local at 192.168.1.XXX

# Завантажити через OTA
pio run --target upload --upload-port ESP32-Bluetti-EB3A.local
# або за IP
pio run --target upload --upload-port 192.168.1.XXX
```

### Метод 2: Arduino IDE

1. Відкрийте Arduino IDE
2. **Tools** → **Port**
3. Виберіть **ESP32-Bluetti-EB3A at 192.168.1.XXX**
4. Натисніть **Upload**

### Метод 3: Makefile

```bash
# Додамо команду в Makefile
make ota IP=192.168.1.XXX
```

---

## 🔧 Налаштування hostname

За замовчуванням: **ESP32-Bluetti-EB3A**

Змінити в `src/main.cpp`:

```cpp
otaHandler->begin("ESP32-Bluetti-EB3A");  // Ваше ім'я
```

**Приклади:**
- `ESP32-Bluetti-EB3A-Living-Room`
- `Bluetti-Bridge-01`
- `EB3A-Gateway`

---

## 📋 Процес оновлення

### Що відбувається:

1. **Початок оновлення**
   ```
   OTA Update Started: firmware
   ```
   - Дисплей увімкнеться автоматично
   - Покаже "OTA Update..."

2. **Процес завантаження**
   ```
   Updating: 25%
   Updating: 50%
   Updating: 75%
   ```
   - Прогрес відображається на дисплеї
   - Логи в Serial Monitor

3. **Завершення**
   ```
   OTA Update Complete!
   Update OK! Reboot...
   ```
   - ESP32 автоматично перезавантажиться
   - Нова прошивка запрацює

### Тривалість:
- **Завантаження:** ~30-60 секунд
- **Перезавантаження:** ~5 секунд
- **Загалом:** ~1 хвилина

---

## 🛡️ Безпека

### Без пароля (за замовчуванням)

Будь-хто в вашій WiFi мережі може оновити прошивку.

### З паролем (рекомендовано)

В `src/ota_handler.cpp`, функція `begin()`:

```cpp
void OTAHandler::begin(const char* hostname) {
    ArduinoOTA.setHostname(hostname);
    
    // Додайте пароль
    ArduinoOTA.setPassword("your_ota_password");  // ⬅️ Додати
    
    // Решта коду...
}
```

При завантаженні через PlatformIO:

```ini
# platformio.ini
upload_port = ESP32-Bluetti-EB3A.local
upload_flags =
    --auth=your_ota_password
```

Або в командному рядку:

```bash
pio run --target upload --upload-port ESP32-Bluetti-EB3A.local --upload-flags="--auth=your_ota_password"
```

---

## 🌐 Знайти IP адресу ESP32

### Метод 1: Serial Monitor

```bash
pio device monitor
```

Шукайте рядок:
```
WiFi connected!
IP address: 192.168.1.XXX
```

### Метод 2: Роутер

Зайдіть в адмін панель роутера → Connected Devices → знайдіть `ESP32-Bluetti-EB3A`

### Метод 3: mDNS (Linux/Mac)

```bash
ping ESP32-Bluetti-EB3A.local
```

### Метод 4: Сканування мережі

```bash
nmap -sn 192.168.1.0/24 | grep -B 2 "ESP32"
```

---

## 📱 Моніторинг під час OTA

### Serial Monitor через WiFi

```bash
# Після OTA оновлення
pio device monitor
```

### Логи в MQTT

OTA процес публікується в MQTT (опціонально):

```cpp
// В ota_handler.cpp можна додати:
mqttClient.publish("homeassistant/bluetti/eb3a/ota/status", "updating");
```

---

## ❌ Усунення проблем

### Не знаходить ESP32 для OTA

**Симптоми:**
```
Error: No upload port specified
```

**Рішення:**

1. **Перевірити що ESP32 підключений до WiFi**
   ```bash
   ping ESP32-Bluetti-EB3A.local
   ```

2. **Використати IP замість hostname**
   ```bash
   pio run --target upload --upload-port 192.168.1.XXX
   ```

3. **Перевірити що OTA ініціалізовано**
   - Подивіться логи: `OTA Ready`
   - Hostname: `ESP32-Bluetti-EB3A`

4. **Перезавантажити ESP32**

### OTA падає з помилкою

**Помилки:**

| Код | Опис | Рішення |
|-----|------|---------|
| `OTA_AUTH_ERROR` | Неправильний пароль | Перевірити пароль |
| `OTA_BEGIN_ERROR` | Не вдалося почати | Перезавантажити ESP32 |
| `OTA_CONNECT_ERROR` | Проблема з'єднання | Перевірити WiFi |
| `OTA_RECEIVE_ERROR` | Помилка отримання | Повторити спробу |
| `OTA_END_ERROR` | Помилка завершення | Перевірити розмір прошивки |

### Прошивка занадто велика

**Симптоми:**
```
Error: Not enough space
```

**Рішення:**

Перевірте partition table (`default_16MB.csv`):
```
app0,     app,  ota_0,   0x10000, 0x640000,  # 6.4MB
app1,     app,  ota_1,   0x650000,0x640000,  # 6.4MB
```

Розмір прошивки має бути < 6.4MB

### OTA повільний

**Нормальна швидкість:** 30-60 секунд

**Якщо повільніше:**
- Слабкий WiFi сигнал
- Багато пристроїв в мережі
- Перешкоди

**Рішення:**
- Розмістити ESP32 ближче до роутера
- Використати 2.4GHz WiFi (не 5GHz)

---

## 🔄 Rollback (відкат до попередньої версії)

ESP32 має **2 OTA партиції** (app0 та app1):

### Автоматичний rollback

Якщо нова прошивка не працює (не запускається), ESP32 автоматично повернеться до попередньої.

### Ручний rollback

В `src/main.cpp`, в `setup()`:

```cpp
#include <Update.h>
#include <esp_ota_ops.h>

void setup() {
    Serial.begin(115200);
    
    // Відкат до попередньої прошивки
    const esp_partition_t* partition = esp_ota_get_boot_partition();
    const esp_partition_t* next = esp_ota_get_next_update_partition(partition);
    esp_ota_set_boot_partition(next);
    ESP.restart();
}
```

---

## 📊 Статистика OTA

| Параметр | Значення |
|----------|----------|
| Розмір прошивки | ~1.2 MB |
| Час завантаження | 30-60 сек |
| Швидкість | ~50-100 KB/s |
| Використання RAM | ~50 KB (під час OTA) |
| Підтримка rollback | Так (автоматично) |

---

## 🎮 Розширені можливості

### OTA через інтернет (не локальна мережа)

Потребує:
1. VPN (WireGuard, OpenVPN)
2. Або порт forwarding на роутері (небезпечно!)
3. Або сервіс типу ngrok

**Не рекомендую** без належної безпеки!

### OTA з веб сервера

```cpp
#include <HTTPUpdate.h>

void checkForUpdate() {
    t_httpUpdate_return ret = httpUpdate.update(client, 
        "http://myserver.com/firmware.bin");
        
    if (ret == HTTP_UPDATE_OK) {
        Serial.println("Update successful!");
        ESP.restart();
    }
}
```

### OTA з GitHub Releases

Автоматично завантажувати нові версії з GitHub:

```cpp
const char* githubUrl = "https://github.com/user/repo/releases/latest/download/firmware.bin";
httpUpdate.update(client, githubUrl);
```

---

## 🔐 Рекомендації безпеки

1. ✅ **Встановіть пароль OTA**
   ```cpp
   ArduinoOTA.setPassword("strong_password");
   ```

2. ✅ **Змініть hostname на унікальний**
   ```cpp
   ArduinoOTA.setHostname("EB3A-12345");
   ```

3. ✅ **Використовуйте окрему WiFi мережу** (гостьова)

4. ✅ **Регулярно оновлюйте прошивку**

5. ❌ **НЕ відкривайте порти в інтернет** без VPN

---

## 📝 Приклади використання

### Щоденна розробка

```bash
# Редагуємо код
nano src/main.cpp

# OTA оновлення
make ota

# Дивимось логи
make monitor
```

### Оновлення на виробництві

```bash
# Збираємо локально
make build

# Перевіряємо що ESP32 доступний
ping ESP32-Bluetti-EB3A.local

# OTA оновлення
pio run --target upload --upload-port ESP32-Bluetti-EB3A.local

# Моніторимо
pio device monitor
```

### Масове оновлення (кілька пристроїв)

```bash
#!/bin/bash
devices=("192.168.1.100" "192.168.1.101" "192.168.1.102")

for ip in "${devices[@]}"; do
    echo "Updating $ip..."
    pio run --target upload --upload-port $ip
done
```

---

## 🆘 Аварійне відновлення

Якщо OTA зламав прошивку і ESP32 не запускається:

1. **Підключіть USB кабель**
2. **Завантажте робочу версію через USB**
   ```bash
   pio run --target upload
   ```
3. **Або використайте esptool.py**
   ```bash
   esptool.py --chip esp32 erase_flash
   pio run --target upload
   ```

**Порада:** Тримайте завжди під рукою USB кабель! 🔌

---

**OTA готовий до використання!** 🎉

Тепер можете оновлювати прошивку через WiFi без USB кабелю!
