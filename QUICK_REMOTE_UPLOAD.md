# 🚀 Швидке віддалене завантаження

## Налаштування

**Сервер:** `11.18.10.40`  
**Логін:** `userr`  
**Порт ESP32:** `/dev/ttyACM0`

## Використання

### Просто запустіть скрипт:

```bash
cd /home/kekcick/Documents/esp32
./remote_upload.sh
```

Скрипт автоматично:
1. ✅ Підключиться до сервера `userr@11.18.10.40`
2. ✅ Створить SSH тунель для `/dev/ttyACM0`
3. ✅ Завантажить прошивку через PlatformIO

## Якщо потрібно змінити параметри

### Через змінні середовища:

```bash
export REMOTE_USER="userr"
export REMOTE_PORT="/dev/ttyACM0"
./remote_upload.sh
```

### Або відредагуйте скрипт:

```bash
nano remote_upload.sh
# Змініть:
# USER="userr"
# REMOTE_PORT="/dev/ttyACM0"
```

## Перше використання

### 1. Перевірте SSH доступ:

```bash
ssh userr@11.18.10.40 "echo 'Connected!'"
```

### 2. Якщо потрібен пароль, налаштуйте SSH ключі:

```bash
# Генеруйте ключ (якщо немає)
ssh-keygen -t rsa -b 4096

# Скопіюйте на сервер
ssh-copy-id userr@11.18.10.40
```

### 3. Перевірте порт на сервері:

```bash
ssh userr@11.18.10.40 "ls -la /dev/ttyACM0"
```

### 4. Перевірте права доступу:

```bash
ssh userr@11.18.10.40 "groups | grep dialout"
# Якщо немає в групі dialout:
ssh userr@11.18.10.40 "sudo usermod -a -G dialout userr"
# Потрібно перезайти в SSH
```

## Якщо щось не працює

### Помилка: "Permission denied" для /dev/ttyACM0

**Рішення:**
```bash
ssh userr@11.18.10.40
sudo usermod -a -G dialout userr
# Перезайдіть в SSH
```

### Помилка: "Connection refused"

**Перевірте:**
- Правильність IP: `11.18.10.40`
- SSH доступ: `ssh userr@11.18.10.40`
- Порт ESP32: `ssh userr@11.18.10.40 "ls /dev/ttyACM*"`

### Помилка: "socat not found"

**Встановіть:**
```bash
sudo apt-get install socat
```

---

**Готово! Тепер просто запускайте `./remote_upload.sh`** ✅

