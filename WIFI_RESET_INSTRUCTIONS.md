# 🔧 Інструкція: Як скинути налаштування WiFi

## Проблема
Якщо ви неправильно ввели назву WiFi (SSID) або пароль, ESP32 не зможе підключитися до WiFi. ESP32 автоматично створить точку доступу (AP) після 10 хвилин, але ви можете зробити це швидше.

---

## ✅ Рішення 1: Через Serial Monitor (НАЙШВИДШЕ)

### ⚠️ ВАЖЛИВО: PlatformIO Monitor не підтримує введення!

Стандартний `pio device monitor` **тільки показує** вивід, але **не дозволяє вводити** команди.

### Варіант A: Використати `screen` (рекомендовано)

#### Крок 1: Встановіть screen (якщо немає)
```bash
# Arch Linux / Manjaro
sudo pacman -S screen

# Ubuntu / Debian
sudo apt install screen
```

#### Крок 2: Підключіть ESP32 до комп'ютера
Підключіть ESP32 через USB кабель.

#### Крок 3: Знайдіть порт
```bash
pio device list
```
Або:
```bash
ls /dev/ttyACM* /dev/ttyUSB* 2>/dev/null
```
Зазвичай це `/dev/ttyACM0` або `/dev/ttyUSB0`

#### Крок 4: Відкрийте screen
```bash
screen /dev/ttyACM0 115200
```
(Замініть `/dev/ttyACM0` на ваш порт)

#### Крок 5: Введіть команду
Введіть одну з команд і натисніть Enter:

**Для скидання тільки WiFi:**
```
resetwifi
```

**Для примусового запуску AP режиму:**
```
ap
```

**Для перегляду статусу:**
```
status
```

**Для перегляду всіх команд:**
```
help
```

#### Крок 6: Вийти з screen
Натисніть: `Ctrl + A`, потім `K`, потім `Y` (підтвердити)

---

### Варіант B: Використати `picocom`

#### Крок 1: Встановіть picocom
```bash
# Arch Linux / Manjaro
sudo pacman -S picocom

# Ubuntu / Debian
sudo apt install picocom
```

#### Крок 2: Підключіть ESP32

#### Крок 3: Відкрийте picocom
```bash
picocom -b 115200 /dev/ttyACM0
```
(Замініть `/dev/ttyACM0` на ваш порт)

#### Крок 4: Введіть команду
Введіть команду і натисніть Enter.

#### Крок 5: Вийти з picocom
Натисніть: `Ctrl + A`, потім `Ctrl + X`

---

### Варіант C: Використати `minicom`

#### Крок 1: Встановіть minicom
```bash
# Arch Linux / Manjaro
sudo pacman -S minicom

# Ubuntu / Debian
sudo apt install minicom
```

#### Крок 2: Налаштуйте minicom
```bash
sudo minicom -s
```
Виберіть:
- `Serial port setup`
- Встановіть `Serial Device: /dev/ttyACM0` (або ваш порт)
- Встановіть `Bps/Par/Bits: 115200 8N1`
- Збережіть налаштування (`Save setup as dfl`)
- Вихід (`Exit`)

#### Крок 3: Запустіть minicom
```bash
minicom
```

#### Крок 4: Введіть команду
Введіть команду і натисніть Enter.

#### Крок 5: Вийти з minicom
Натисніть: `Ctrl + A`, потім `X`, потім `Enter`

---

### Крок 7: Очікуйте перезапуск
Після команди `resetwifi` ESP32 автоматично перезапуститься через 2 секунди і використає значення з `secrets.h`.

**Для скидання тільки WiFi:**
```
resetwifi
```
або
```
clearwifi
```

**Для примусового запуску AP режиму (не чекаючи 10 хвилин):**
```
ap
```
або
```
startap
```

**Для перегляду поточного статусу:**
```
status
```

**Для перегляду всіх доступних команд:**
```
help
```

### Крок 4: Очікуйте перезапуск
Після команди `resetwifi` ESP32 автоматично перезапуститься через 2 секунди і використає значення з `secrets.h`.

---

## ✅ Рішення 2: Через файл secrets.h

### Крок 1: Відкрийте файл
```bash
nano include/secrets.h
```
або через ваш редактор коду.

### Крок 2: Змініть значення
Знайдіть рядки:
```cpp
static constexpr char WIFI_SSID[] = "Robinson WiFi";
static constexpr char WIFI_PASSWORD[] = "0504479307qw";
```

Змініть на правильні значення:
```cpp
static constexpr char WIFI_SSID[] = "Ваша_назва_WiFi";
static constexpr char WIFI_PASSWORD[] = "Ваш_пароль";
```

### Крок 3: Скиньте налаштування через Serial
Відкрийте Serial Monitor і введіть:
```
resetwifi
```

### Крок 4: Завантажте прошивку
```bash
pio run --target upload
```

---

## ✅ Рішення 2: Через скрипт (НАЙПРОСТІШЕ!)

### Крок 1: Підключіть ESP32 до комп'ютера

### Крок 2: Використайте скрипт
```bash
cd ~/Documents/esp32
./send_command.sh resetwifi
```

**Доступні команди:**
```bash
./send_command.sh resetwifi    # Скинути WiFi
./send_command.sh ap           # Запустити AP
./send_command.sh status       # Показати статус
./send_command.sh help         # Показати допомогу
```

**Переваги:**
- ✅ Не потрібно встановлювати додаткові програми
- ✅ Просто і швидко
- ✅ Автоматично знаходить порт

---

## ✅ Рішення 3: Через веб-інтерфейс (якщо AP працює)

### Крок 1: Підключіться до AP
- SSID: `esp32`
- Password: `12345678`

### Крок 2: Відкрийте браузер
Перейдіть на: `http://192.168.4.1`

### Крок 3: Перейдіть в налаштування
Натисніть кнопку **"⚙️ Налаштування"**

### Крок 4: Введіть правильні дані
- **WiFi SSID:** Ваша назва WiFi
- **WiFi Password:** Ваш пароль

### Крок 5: Збережіть
Натисніть **"💾 Save"** - ESP32 автоматично перезапуститься.

---

## ✅ Рішення 4: Через файл secrets.h

### Крок 1: Відкрийте файл
```bash
nano include/secrets.h
```
або через ваш редактор коду.

### Крок 2: Змініть значення
Знайдіть рядки:
```cpp
static constexpr char WIFI_SSID[] = "Robinson WiFi";
static constexpr char WIFI_PASSWORD[] = "0504479307qw";
```

Змініть на правильні значення:
```cpp
static constexpr char WIFI_SSID[] = "Ваша_назва_WiFi";
static constexpr char WIFI_PASSWORD[] = "Ваш_пароль";
```

### Крок 3: Скиньте налаштування через Serial
Відкрийте Serial Monitor (через `screen`, `picocom` або `minicom`) і введіть:
```
resetwifi
```

### Крок 4: Завантажте прошивку
```bash
pio run --target upload
```

---

## ✅ Рішення 5: Повне скидання всіх налаштувань

Якщо потрібно скинути **всі** налаштування (WiFi, MQTT, Bluetti MAC):

### Через Serial Monitor:
```
resetconfig
```
або
```
clearconfig
```

**УВАГА:** Це видалить всі збережені налаштування!

---

## 🔍 Діагностика

### Перевірка поточного статусу
Введіть в Serial Monitor:
```
status
```

Ви побачите:
```
=== Current Status ===
WiFi SSID: Robinson WiFi
WiFi Connected: No
AP Mode: No
Uptime: 123 seconds
```

### Якщо AP не запускається після 10 хвилин

1. **Перевірте Serial Monitor** - можуть бути помилки
2. **Примусово запустіть AP:**
   ```
   ap
   ```
3. **Перевірте, чи правильно встановлено `wifiConnectStartTime`** - він повинен бути встановлений при старті

---

## 📝 Доступні команди

| Команда | Опис |
|---------|------|
| `resetwifi` | Скинути тільки WiFi SSID та пароль |
| `clearwifi` | Те саме, що `resetwifi` |
| `resetconfig` | Скинути всі налаштування |
| `clearconfig` | Те саме, що `resetconfig` |
| `ap` | Примусово запустити AP режим |
| `startap` | Те саме, що `ap` |
| `status` | Показати поточний статус |
| `info` | Те саме, що `status` |
| `help` | Показати всі доступні команди |

---

## ⚠️ Важливо

1. **Після `resetwifi`** ESP32 використає значення з `secrets.h`
2. **Якщо в `secrets.h` неправильні дані**, змініть їх перед скиданням
3. **AP режим** автоматично запускається через 10 хвилин після початку спроб підключення
4. **Через веб-інтерфейс** можна змінити налаштування без перезапуску (крім WiFi)

---

## 🐛 Виправлення проблеми з AP режимом

Якщо AP не запускається після 10 хвилин, можлива проблема була в тому, що `wifiConnectStartTime` не був правильно ініціалізований. Це виправлено в коді:

```cpp
// Тепер перевіряємо, чи wifiConnectStartTime був встановлений
unsigned long timeSinceStart = (wifiConnectStartTime > 0) ? (millis() - wifiConnectStartTime) : 0;

if (wifiConnectStartTime > 0 && timeSinceStart > WIFI_TIMEOUT_MS) {
    startAPMode();
}
```

---

## 📞 Швидка допомога

**Якщо нічого не працює:**

1. Завантажте нову прошивку (з виправленнями)
2. Відкрийте Serial Monitor
3. Введіть `resetwifi`
4. Перевірте `secrets.h` - чи правильні там дані
5. Якщо AP не запускається, введіть `ap`

---

**Готово!** Тепер ви можете легко скинути налаштування WiFi! 🚀

